# フォーマット統一ツール

会社ごとに列の並びがバラバラな顧客リストExcelファイルを、統一されたフォーマットの1つのファイルにまとめるツールです。

## 解決する課題

複数の取引先から提供される顧客リストは、会社ごとに列の順番(会社名・担当者名・電話番号・メールアドレスの並び)がバラバラなことが多く、そのまま1つの表にまとめることができません。このツールは、各ファイルのヘッダー行を自動で解析し、列の並びが違っていても正しい項目に統一します。

## 主な機能

- フォルダ内の複数Excelファイルを自動で一括読み込み
- 各ファイルのヘッダー行を解析し、「どの項目が何列目にあるか」を自動判定(列の並び順に依存しない)
- 会社名・担当者名・電話番号・メールアドレスの統一フォーマットで1つのリストに集約
- 統一されたExcelファイルとして出力

## 使い方

1. `顧客データ`フォルダの中に、会社ごとの顧客リストファイルを入れる(4項目:会社名・担当者名・電話番号・メールアドレスを含み、列の並び順は問わない)
2. 以下を実行

```bash
python format_touitsu.py
```

3. `顧客リスト_統一.xlsx`が生成される(全社分のデータが統一フォーマットでまとまる)

## サンプルデータ

`顧客データ`フォルダに、列の並びが異なる3社分(A社・B社・C社)のサンプルExcelファイルを同梱しています。

## 使用技術

- Python 3
- openpyxl
